@using CutIt.Models

<div>
    @using (Html.BeginForm("Create", "Link", FormMethod.Post))
    {
        @Html.Editor("OriginalLink");
        <button type="submit">Create</button><br/>
        @Html.ValidationMessage("OriginalLink")
    }
</div>